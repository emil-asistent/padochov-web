server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # relativní Location -> nerozbije se https za Cloudflare tunelem
    absolute_redirect off;
    port_in_redirect off;

    gzip on; gzip_vary on; gzip_min_length 1024;
    gzip_types text/plain text/css text/javascript application/javascript application/json image/svg+xml;

    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # poptávkový formulář -> node (server.js)
    location ^~ /api/ {
        client_max_body_size 128k;
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
        proxy_read_timeout 30s;
    }

    # čisté URL: /kontakt.html -> /kontakt (stejně jako cleanUrls na Vercelu)
    location = /index.html { return 308 /; }
    location ~ ^/(.+)\.html$ { return 308 /$1; }

    # odkaz zkopírovaný s koncovým lomítkem musí dojít na stejnou stránku, ne na 404
    location ~ ^/(.+)/$ { return 301 /$1; }

    # vlastní 404 ve vzhledu webu (musí mít absolutní cesty k assetům)
    error_page 404 /404.html;
    location = /404.html { internal; }

    # POZOR: add_header v location bloku ruší add_header ze server bloku, proto se
    # bezpečnostní hlavičky musí v každém bloku, který si nastavuje cache, zopakovat.

    # video se může měnit -> kratší cache, bez immutable
    location ~* \.(mp4|webm)$ {
        expires 7d; add_header Cache-Control "public"; try_files $uri =404;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }

    location ~* \.(jpg|jpeg|png|gif|webp|svg|css|js|woff|woff2|ttf|ico|pdf)$ {
        expires 30d; add_header Cache-Control "public, immutable"; try_files $uri =404;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }

    # textové soubory pro roboty se mění s obsahem webu -> krátká cache, ať se
    # po deployi nedrží stará verze (Cloudflare navíc čisti ručně)
    location ~* ^/(robots\.txt|sitemap\.xml|llms\.txt)$ {
        add_header Cache-Control "public, max-age=0, s-maxage=600, stale-while-revalidate=86400" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        try_files $uri =404;
    }

    # HTML: prohlížeč si vždy ověří aktuálnost, edge (Cloudflare) může držet 10 minut.
    # Bez téhle hlavičky se stránky vracely úplně bez Cache-Control a každý mezičlánek
    # si dobu platnosti odhadoval po svém.
    location / {
        add_header Cache-Control "public, max-age=0, s-maxage=600, stale-while-revalidate=86400" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        try_files $uri $uri.html $uri/index.html $uri/ =404;
    }
}
